{
  "name": "@sevenvid/ipc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed IPC contract (zod) shared by main, preload, renderer and the dev bridge",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@sevenvid/core": "workspace:*",
    "zod": "^4.1.0"
  }
}
